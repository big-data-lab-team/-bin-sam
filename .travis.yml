dist: trusty
language: python

python:
    - 3.4
    - 3.5
    - 3.6
    - nightly

matrix:
  include:
    - python: 3.7
      dist: xenial
      sudo: true

install:
    - pip install coveralls pytest==3.6 pytest-cov pycodestyle nibabel

script:
    - python -m pytest

after_success:
    - coveralls
